<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Labs20265Gr12">

        <!-- PersonalDataActivity tiene el intent-filter que la hace la principal -->
        <activity
            android:name=".PersonalDataActivity"
            android:exported="true"
            android:label="@string/title_activity_personal_data"
            android:theme="@style/Theme.Labs20265Gr12"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- NUEVO: Registro de ContactDataActivity -->
        <activity
            android:name=".ContactDataActivity"
            android:exported="false"
            android:label="@string/titulo_info_contacto"
            android:theme="@style/Theme.Labs20265Gr12"
            android:windowSoftInputMode="adjustResize">
        </activity>

    </application>

</manifest>